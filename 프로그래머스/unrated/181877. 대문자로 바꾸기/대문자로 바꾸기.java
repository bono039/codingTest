import java.util.*;
import java.io.*;

class Solution {
    public String solution(String myString) {
        StringBuilder sb = new StringBuilder();
        sb.append(myString.toUpperCase());
        return sb.toString();
    }
}